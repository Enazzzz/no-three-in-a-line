# Next agent checklist

1. Read all of:
   - `docs/FINDINGS.md`
   - `docs/FINDINGS_MULTI_HYPERBOLA.md`
   - `docs/FINDINGS_PRIMARY_REPAIR.md`
   - `docs/FINDINGS_ALLSLOPE_HITTING.md`
2. Do **not** redo saturated paths:
   - HJSW polish / second-hyperbola grafts onto S₂
   - unstructured delete-first on raw multi-hyperbola unions
   - exact primary packing hoping repair preserves surplus
   - (new) hoping better all-slope repair saves primary surplus for `p≥17` —
     an LB already exceeds surplus
3. Higher-leverage next attempts:
   - structured masks / residue schedules that **cap non-primary line loads
     by algebra**
   - prove `LB(p) ≥ surplus(p)` for these pools for all odd `p≥17`
4. Commit and push frequently — cloud VMs wipe uncommitted work.
5. Do not claim an asymptotic `>1.55n` breakthrough without a real construction or proof.
