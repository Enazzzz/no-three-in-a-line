# Next agent checklist

1. Read **`docs/FINDINGS_SUMMARY.md`**, then `PROOF_*.md` as needed.
2. **Primary goal:** a counting proof of Conjecture B (`LB ≥ surplus` on
   board max-primary multi-`H`), or of B1 plus a gap that does **not** use
   mono-cap→B2. See B2 provenance negative result in `PROOF_LB_SURPLUS.md`.
3. Do **not** redo saturated construction paths — dead-end table in
   `FINDINGS_SUMMARY.md`.
4. Do **not** cite “mono ≈ 0.7 · HJSW” as single-H structure; that ratio is
   `∼2p/3(p−1)` from a separate ±1 packing size, and does not imply B2.
5. Optional side theorem: Conjecture M (mono primary cap by `p mod 4`).
6. Commit/push often; prefer `main` over PRs unless asked.
7. Fair baselines if constructing: multi-seed polished HJSW; ambient pools if seeding S₂.
