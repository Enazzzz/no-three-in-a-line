# Next agent checklist

1. Read **`docs/FINDINGS_SUMMARY.md`**, then `PROOF_*.md` as needed.
2. **Primary goal:** turn certified claims into theorems. Top target is
   Conjecture B via lemmas **B1/B2** in `docs/PROOF_LB_SURPLUS.md`
   (`data/conjecture_b_lemmas_scan.csv`).
3. Do **not** redo saturated construction paths (grafts, delete-first,
   ambient multi-`H` polish, same-`p` board growth, etc.) — see the dead-end
   table in `FINDINGS_SUMMARY.md`.
4. Secondary (only if theorem work is blocked): new algebraic families /
   moduli co-designed with `n`.
5. Commit and push frequently; prefer landing on `main` over opening PRs unless asked.
6. Do not claim an asymptotic `>1.55n` breakthrough without a construction or proof.
7. Fair baselines: multi-seed polished HJSW; ambient-aligned pools if the seed is S₂.
